{
  "name": "vite-jjsx-ssr",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "bun --inspect=6499 src/app.ts",
    "start": "cross-env NODE_ENV=production bun --inspect=6499 src/app.ts",
    "build": "bun build:server && bun build:client",
    "build:client": "vite build --config vite.config.client.ts",
    "build:server": "vite build --config vite.config.server.ts",
    "build:static": "cross-env NODE_ENV=production carats-ssg",
    "preview": "cross-env NODE_ENV=production bun dist/app.js",
    "test": "vitest --run --coverage"
  },
  "dependencies": {
    "@carats/core": "^0.0.15",
    "@carats/csr": "^0.0.28",
    "@carats/express": "^0.0.18",
    "@carats/hooks": "^0.0.2",
    "@carats/render": "^0.0.16",
    "@carats/ssg": "^0.0.9",
    "@carats/ssr": "^0.0.25",
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "jjsx": "^4.0.3",
    "sass-embedded": "^1.99.0",
    "sirv": "^3.0.2"
  },
  "devDependencies": {
    "@fetch-mock/vitest": "^0.2.18",
    "@types/bun": "^1.3.11",
    "@types/compression": "^1.8.1",
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.2",
    "@vitest/coverage-v8": "4.0.18",
    "cross-env": "^10.1.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.8",
    "vitest": "^4.1.3"
  }
}