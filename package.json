{
  "name": "vite-jjsx-ssr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --inspect=6499 server.js",
    "build": "npm run build:server && npm run build:client",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --ssr src/entry-server.ts --outDir dist/server",
    "preview": "cross-env NODE_ENV=production node server"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "jjsx": "^3.1.0",
    "sass": "^1.97.3",
    "sirv": "^3.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.2",
    "cross-env": "^10.1.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.7"
  }
}